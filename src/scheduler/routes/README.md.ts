These are **templates** for Next.js App Router route handlers.

Claude Code should:
- place them into the repo’s actual `app/api/.../route.ts` structure
- wire auth + tenant resolution to existing project utilities
- replace TODO stubs
